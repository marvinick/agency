<ul class="nav">
  <li>
    <%= link_to "Agency", root_path, :class => 'brand' %>
  </li>

<div class='btn-group nav_item'>
  <ul class='dropdown-menu'>
    <% Skill.all.each do |skill| %>
      <li>
        <%= link_to skill.name, skill_path(skill) %>
      </li>
    <% end %>
    <li class='divider'></li>
    <li>
      <%= link_to "Add Skill", new_skill_path %>
    </li>
  </ul>
</div>
